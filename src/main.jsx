import React from "react";
import ReactDOM from "react-dom/client";
import GRIPCRM from "../GRIP-CRM.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GRIPCRM />
  </React.StrictMode>
);
